<div class="passwordBox animated fadeInDown">
    <div class="row">

        <div class="col-md-12">
            <div class="ibox-content">

                <h2 class="font-bold">Olvidaste tu contraseña?</h2>

                <p>
                    Ingrese su dirección de correo electrónico y su contraseña será restablecida y le será enviada por correo electrónico.
                </p>

                <div class="row">
                    <div class="col-lg-12">
                        <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                          <%= render "devise/shared/error_messages", resource: resource %>

                              <div class="form-group">
                                <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
                              </div>

                              <%= f.submit "Enviarme instrucciones para restablecer contraseña", class: "btn btn-primary block full-width m-b" %>
                        <%end%>
                        <center><%= render "devise/shared/links" %></center>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <hr/>
    <div class="row">
        <div class="col-md-6">
            Company CarsOk
        </div>
        <div class="col-md-6 text-right">
            <small>© 2015</small>
        </div>
    </div>
</div>
